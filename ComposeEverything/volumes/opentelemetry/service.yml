service:
  pipelines:
    metrics:
      receivers: [prometheus/user]
      processors: [batch]
      exporters: [prometheus]
    logs:
      receivers: [filelog/containers]
      processors: [batch]
      exporters: [logging, loki]